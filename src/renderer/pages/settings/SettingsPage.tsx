/**
 * Settings route (`/settings`).
 *
 * A route rather than a modal so future options have room to grow
 * (`docs/specs/v1.0/renderer/routing-layout.md`). Placeholder shell — the
 * theme / language / library controls are implemented in Phase 7.
 */
export const SettingsPage = () => (
  <section className="p-6">
    <h1 className="text-lg font-semibold">Settings</h1>
    <p className="mt-2 text-sm text-muted-foreground">
      設定画面は Phase 7 で実装します。
    </p>
  </section>
);
