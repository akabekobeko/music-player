import { PageContent } from "./components/PageContent";

/**
 * Playlist view route (`/playlists/:playlistId?`); the implementation lives
 * in `PageContent`.
 */
export const PlaylistsPage = () => <PageContent />;
