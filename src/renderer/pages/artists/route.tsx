import { PageContent } from "./components/PageContent";

/**
 * Artist view route (`/artists/:artistName?`); the implementation lives in
 * `PageContent`.
 */
export const ArtistsPage = () => <PageContent />;
