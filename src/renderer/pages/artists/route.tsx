import { PageContent } from "./components/PageContent";

/**
 * Artist view route (`/artists`, `/artists/unknown`,
 * `/artists/name/:artistName`); the implementation lives in `PageContent`.
 */
export const ArtistsPage = () => <PageContent />;
