/**
 * Album view route (`/albums`).
 *
 * Placeholder shell — the filter UI and virtualised album grid are
 * implemented in Phase 5 (issue: Album ビュー).
 */
export const AlbumsPage = () => (
  <section className="p-6">
    <h1 className="text-lg font-semibold">Albums</h1>
    <p className="mt-2 text-sm text-muted-foreground">
      アルバムビューは Phase 5 で実装します。
    </p>
  </section>
);
