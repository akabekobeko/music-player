/**
 * Classes shared by every tab panel. Each panel pads itself instead of the
 * dialog body so a scrolling panel puts its scrollbar at the popup edge
 * (see `DialogContent`).
 */
export const panelClassName = "min-h-0 px-4 pb-4";
