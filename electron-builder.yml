appId: me.akabeko.parade
productName: Parade
directories:
  output: release
  buildResources: build
files:
  - dist/**/*
  - "!node_modules/**/*"
asar: true
mac:
  target:
    - dmg
    - zip
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
win:
  target:
    - nsis
    - zip
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
linux:
  target:
    - AppImage
    - deb
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
