appId: me.akabeko.music-player
productName: MusicPlayer
directories:
  output: release
  buildResources: build
files:
  - dist/**/*
  - "!node_modules/**/*"
asar: true
mac:
  target:
    - dmg
    - zip
  artifactName: "${productName}-${version}-${arch}.${ext}"
win:
  target:
    - nsis
    - zip
  artifactName: "${productName}-${version}-${arch}.${ext}"
linux:
  target:
    - AppImage
    - deb
  artifactName: "${productName}-${version}-${arch}.${ext}"
